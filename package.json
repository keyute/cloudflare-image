{
  "name": "cloudflare-image-resize",
  "version": "0.1.3",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cp package.json README.md ./dist"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "@types/react": "^18.2.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^4.9.5"
  },
  "keywords": [
    "cloudflare",
    "image",
    "resize",
    "typescript"
  ]
}
